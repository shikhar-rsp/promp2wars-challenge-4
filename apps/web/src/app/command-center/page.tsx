import { CommandCenter } from '@/features/command-center/CommandCenter';

export const metadata = { title: 'Command Center · ATLAS' };

export default function CommandCenterPage() {
  return <CommandCenter />;
}
